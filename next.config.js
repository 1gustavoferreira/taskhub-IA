/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // adicione outras configs aqui, sem tipos TS
  // exemplo:
  // images: { domains: ["localhost"] },
  // experimental: { serverActions: true },
};

module.exports = nextConfig;

